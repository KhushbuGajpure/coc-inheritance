#include <bits/stdc++.h>
#define 11 long long
using namespacestd;
int main(){
    ll n=2;
    cout<<n;
    vector<int> v={2,3,5,6};
   // for{int i=0; i<4; i+++}
   // cout<<v[i]<<endl;
   for(int & value:v)
   value++:
   auto it=v.begin();
   
   vector <int> arr(10);
   iota (arr.begin(), arr.end, -5);
   
   sort(arr.rbegin(),arr.rend());
   for(auto i: arr)
   cout<< i;
   cout << endl;
   
   arr.insert(5,1,10))

   


}
